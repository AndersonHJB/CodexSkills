interface:
  display_name: "个人 IP 角色批量生成"
  short_description: "为零基础用户生成含两套浅色盲盒的完整个人 IP 角色系统"
  default_prompt: "Use $generate-personal-ip-avatars with its default all-on configuration to create my complete personal-IP blind box, including two newly designed light-palette series and a collage of every delivered original."
